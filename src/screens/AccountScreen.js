import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const AccountsScreen = () => {
    return <Text>AccountsScreen</Text>;
};

const styles = StyleSheet.create({});

export default AccountsScreen;